Папка windows(Test) — копия для теста на Windows VM.
Просто скопируй эту папку в винду и запусти:
  pip install -r requirements.txt
  python main.py --test
  pyinstaller --onefile --noconsole --name Diskteam main.py
